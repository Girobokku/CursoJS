// String Methods - includes y length

const producto = "Monitor 20 Pulgadas";

console.log(producto);

// Conocer la cantidad de letras de la cadena de texto
console.log(producto.length);

// .indexOf() para saber en qué posición está un elemento

/*
console.log(producto.indexOf("Monitor"));
console.log(producto.indexOf("r"));
*/

// .includes para ver si la cadena de texto contiene una palabra (ES CASE SENSITIVE)
console.log(producto.includes("Monitor"));
